export const getInstructorCardStyles = (theme) => ({
  card: {
    textAlign: 'center',
  },
  avatar: {
    width: 80,
    height: 80,
    margin: 'auto',
    marginTop: theme.spacing(2),
  },
  cardContent: {
    paddingTop: theme.spacing(1.5),
  },
});