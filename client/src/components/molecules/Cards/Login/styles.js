export const styles = {
    card: (theme) => ({
      backgroundColor: theme.palette.grey[100],
      boxShadow: '0 4px 12px rgba(0, 0, 0, 0.15)',
      padding: '24px',
      borderRadius: '10px',
      maxWidth: 400,
      width: '100%',
    }),
  };