export const getCreateLessonPageStyles = (theme) => ({
  wrapper: {
    padding: theme.spacing(3),
    maxWidth: 800,
    margin: 'auto',
    marginTop: theme.spacing(4),
  },
  container: {
    padding: theme.spacing(4),
  },
});