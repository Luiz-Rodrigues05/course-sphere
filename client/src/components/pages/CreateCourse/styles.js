export const getCreateCoursePageStyles = (theme) => ({
  wrapper: {
    padding: theme.spacing(3),
    maxWidth: 800,
    margin: 'auto',
  },
  formContainer: {
    padding: theme.spacing(4),
  },
});