export const getEditCoursePageStyles = (theme) => ({
  wrapper: {
    padding: theme.spacing(3),
    maxWidth: 800,
    margin: 'auto',
    marginTop: theme.spacing(4),
  },
  formContainer: {
    padding: theme.spacing(4),
  },
  feedbackContainer: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    minHeight: '60vh',
  },
});